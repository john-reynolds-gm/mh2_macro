# Core — everything needed to build the database and run the reports.
# This is all most people need. Deliberately small and slow-moving.
pandas>=2.0
openpyxl>=3.1          # .xlsx / .xlsm reading
python-docx>=1.1       # ladder .docx parsing

# The review UI.
streamlit>=1.30

# The review write-path API (review_api.py, Session C).
fastapi>=0.115
uvicorn>=0.30

# Test suite.
pytest>=8.0
